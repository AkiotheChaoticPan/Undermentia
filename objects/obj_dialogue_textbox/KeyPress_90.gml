/// @description Insert description here
if(!visible) {
	visible = true;
	text = "* Hello. Name's Sands. Sands the skeleton. I currently don't have a face, because this is only a first step to a complete dialogue system. This is all a placeholder text for testing, after all.";
	text_lenght = string_length(text);
	text_index = 0;
}